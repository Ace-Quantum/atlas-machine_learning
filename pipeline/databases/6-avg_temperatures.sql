-- They're giving us a database to download
-- Ok so not only do I NOT want to do this
-- But I can't
-- Like, my computer won't install MySQL
-- So that's annoying

SELECT city, AVG(value) as avg_temp
FROM temperatures;