#!/usr/bin/env python3
"""performs forward propogation for a bidirectional RNN
Ok so on that note
Why is this function separate of the class?
Is that just a preference in code architecture?"""

import numpy as np


def bi_rnn(bi_cell, X, h_0, h_t):
    """forward prop"""
    return None
